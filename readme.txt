keyboards:
	-осуществить скрытие клавиатуры
	-постараться сделать автоматическую генерацию кнопок
tg_bot:
		add_budget:
	-из-за парсинга не работает добавление(разобраться с опознованием "+")
-статистика!!!

database команда: sqlite3 db/createdb.db < createdb.sql